# info

launcher: prism
version: 1.21.5
mod loader: fabric
mod: fabric api, baritone, minescript (5.0)
mappings: install by `\install_mappings` (currently pyjinn only support intermediary mapping and not yarn)

# must do

- Change python executable path in `config.txt`

# others

- Search for java class name here: `https://linkie.shedaniel.dev/mappings?namespace=yarn&version=1.21.5`
